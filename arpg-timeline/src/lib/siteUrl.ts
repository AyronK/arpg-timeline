/** Canonical production origin. Used where a stable absolute URL is required
 * (structured data, canonical tags, OG images) and env vars aren't guaranteed. */
export const SITE_URL = "https://www.arpg-timeline.com";

export const DEFAULT_OG_IMAGE = `${SITE_URL}/assets/seoimage.png`;
